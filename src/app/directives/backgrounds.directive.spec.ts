import { BackgroundsDirective } from './backgrounds.directive';

describe('BackgroundsDirective', () => {
  it('should create an instance', () => {
    const directive = new BackgroundsDirective();
    expect(directive).toBeTruthy();
  });
});
